let aste = "*";
let n = 5;
let espaco = "";
let horizontal = ' ';

for (let index = 0; index < n; index++) {
  for (let i = 0; i <= i; j++) {
    if (i < index) {
      horizontal += espaco;
      console.log(horizontal);
    } else {
      horizontal += aste;
    }
  }

  index -= 1;
}